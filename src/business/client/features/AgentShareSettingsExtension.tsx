interface AgentShareSettingsExtensionProps {
  agentId: string;
}

/** Optional settings content supplied by a business implementation. */
const AgentShareSettingsExtension = (_props: AgentShareSettingsExtensionProps) => null;

export default AgentShareSettingsExtension;
